version: 1.0
runtime: corretto11
build:
  commands:
    build:
      - chmod +x mvnw
      - ./mvnw clean package -DskipTests
run:
  command: java -jar target/tichu-sim-1.0.0.jar
  network:
    port: 8080
